# Compiled binary (anchored to repo root so it does NOT match cmd/quic-link/ source dir)
/quic-link

# Go build/test artifacts
*.test
*.out
*.exe

# Local docs / notes (not versioned)
HANDOFF.md
CLAUDE.md
notes/
internal-docs/

# Certificates and private keys (never commit)
certs/
*.key
*.crt
*.csr
*.pem
*.srl

# Carter internal state
.carter/

# Key metadata sidecars
*.pem.meta

# Local rig credentials — NEVER commit (contains plaintext SSH password)
remote-ubuntu.md

# Local rig configuration (machine-specific, not for version control)
agent-config.toml

# Local identity pin (contains a real key pin for this rig)
macos-pin

# Dead Phase-0 CA artifact (openssl helper, superseded by pinning in 1a.4)
openssl-setup.sh
